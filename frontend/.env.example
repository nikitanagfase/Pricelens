# Vite requires the VITE_ prefix (not REACT_APP_ — that's a
# Create-React-App-only convention). Point this at your FastAPI
# backend in production; in local dev, Vite's proxy (vite.config.js)
# handles it automatically so this can stay as-is.
VITE_API_URL=http://localhost:8000
